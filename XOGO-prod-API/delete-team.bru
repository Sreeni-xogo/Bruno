meta {
  name: Delete Team
  type: http
  seq: 73
}

delete {
  url: {{baseUrl}}/v2/api/admin/company-teams/delete/{{teamId}}
  body: none
  auth: none
}

headers {
  x-api-version: {{x-api-version}}
  x-manager-key: {{authToken}}
}

tests {
  test("Status is 200", function() {
    expect(res.getStatus()).to.equal(200);
  });
}
