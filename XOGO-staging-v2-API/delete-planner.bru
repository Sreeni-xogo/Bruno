meta {
  name: Delete Planner
  type: http
  seq: 164
}

delete {
  url: {{baseUrl}}/v2/api/Playlists?id={{crudPlannerId}}
  body: none
  auth: none
}

headers {
  x-api-version: {{x-api-version}}
  x-manager-key: {{authToken}}
}

tests {
  test("Status is 200", function() {
    expect(res.getStatus()).to.equal(200);
  });
}
