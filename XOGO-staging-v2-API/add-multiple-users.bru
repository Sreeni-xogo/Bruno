meta {
  name: Add Multiple Users
  type: http
  seq: 85
}

post {
  url: {{baseUrl}}/v2/api/admin/team-user/add-multiple-users?teamId={{companyTeamId}}&selectedUserIds=
  body: none
  auth: none
}

headers {
  x-api-version: {{x-api-version}}
  x-manager-key: {{authToken}}
}

tests {
  test("Status is 200", function() {
    expect(res.getStatus()).to.equal(200);
  });
}
