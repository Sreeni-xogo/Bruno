meta {
  name: Get Last Disable Teams Request
  type: http
  seq: 153
}

get {
  url: {{baseUrl}}/v2/api/admin/company-settings/last-disable-teams-request-datetime
  body: none
  auth: none
}

headers {
  x-api-version: {{x-api-version}}
  x-manager-key: {{authToken}}
}

tests {
  test("Status is 200", function() {
    expect([200, 204]).to.include(res.getStatus());
  });

  test("Response is not empty", function() {
    // 204 has no body - status check is sufficient
  });
}
