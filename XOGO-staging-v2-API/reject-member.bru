meta {
  name: Reject Member
  type: http
  seq: 82
}

post {
  url: {{baseUrl}}/v2/api/admin/members/reject/{{id}}
  body: none
  auth: none
}

headers {
  x-api-version: {{x-api-version}}
  x-manager-key: {{authToken}}
}

tests {
  test("Status is 204", function() {
    expect(res.getStatus()).to.equal(204);
  });
}
