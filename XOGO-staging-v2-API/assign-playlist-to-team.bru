meta {
  name: Assign Playlist To Team
  type: http
  seq: 68
}

post {
  url: {{baseUrl}}/v2/api/company/teams/{{companyTeamId}}/playlist?playlistId={{playlistId}}
  body: none
  auth: none
}

headers {
  x-api-version: {{x-api-version}}
  x-manager-key: {{authToken}}
}

tests {
  test("Status is 204", function() {
    expect(res.getStatus()).to.equal(204);
  });
}
