meta {
  name: Add Update Player Tags
  type: http
  seq: 45
}

post {
  url: {{baseUrl}}/v2/api/Players/{{playerId}}/TagAddUpdate
  body: json
  auth: none
}

headers {
  Content-Type: application/json
  x-api-version: {{x-api-version}}
  x-manager-key: {{authToken}}
}

body:json {
  {
    "key": "test-tag",
    "value": "test-value"
  }
}

tests {
  test("Status is 200", function() {
    expect(res.getStatus()).to.equal(200);
  });
}
