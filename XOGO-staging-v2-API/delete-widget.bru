meta {
  name: Delete Widget
  type: http
  seq: 162
}

delete {
  url: {{baseUrl}}/v2/api/LibraryItems?id={{crudWidgetId}}
  body: none
  auth: none
}

headers {
  x-api-version: {{x-api-version}}
  x-manager-key: {{authToken}}
}

tests {
  test("Status is 200", function() {
    expect(res.getStatus()).to.equal(200);
  });
}
