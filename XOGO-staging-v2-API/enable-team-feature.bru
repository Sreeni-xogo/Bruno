meta {
  name: Enable Team Feature
  type: http
  seq: 110
}

post {
  url: {{baseUrl}}/v2/api/admin/company-settings/enable-team-feature
  body: none
  auth: none
}

headers {
  x-api-version: {{x-api-version}}
  x-manager-key: {{authToken}}
}

tests {
  test("Status is 200", function() {
    expect(res.getStatus()).to.equal(200);
  });
}
