meta {
  name: Delete Planner Event
  type: http
  seq: 32
}

delete {
  url: {{baseUrl}}/v2/api/Playlists/Planner/Event/{{id}}
  body: none
  auth: none
}

headers {
  x-api-version: {{x-api-version}}
  x-manager-key: {{authToken}}
}

tests {
  test("Status is 200", function() {
    expect(res.getStatus()).to.equal(200);
  });
}
