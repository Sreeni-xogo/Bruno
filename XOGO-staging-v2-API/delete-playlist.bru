meta {
  name: Delete Playlist
  type: http
  seq: 160
}

delete {
  url: {{baseUrl}}/v2/api/Playlists?id={{crudPlaylistId}}
  body: none
  auth: none
}

headers {
  x-api-version: {{x-api-version}}
  x-manager-key: {{authToken}}
}

tests {
  test("Status is 200", function() {
    expect(res.getStatus()).to.equal(200);
  });
}
