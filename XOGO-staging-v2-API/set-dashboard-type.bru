meta {
  name: Set Dashboard Type
  type: http
  seq: 109
}

post {
  url: {{baseUrl}}/v2/api/admin/company-settings/dashboard-type
  body: json
  auth: none
}

headers {
  Content-Type: application/json
  x-api-version: {{x-api-version}}
  x-manager-key: {{authToken}}
}

body:json {
  {
    "dashboardType": "SmallBusiness"
  }
}

tests {
  test("Status is 200", function() {
    expect(res.getStatus()).to.equal(200);
  });
}
