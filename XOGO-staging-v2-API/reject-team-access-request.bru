meta {
  name: Reject Team Access Request
  type: http
  seq: 89
}

post {
  url: {{baseUrl}}/v2/api/admin/team-user/reject-team-access-request/{{requestId}}
  body: none
  auth: none
}

headers {
  x-api-version: {{x-api-version}}
  x-manager-key: {{authToken}}
}

tests {
  test("Status is 200", function() {
    expect(res.getStatus()).to.equal(200);
  });
}
